import styled from 'styled-components';

export const StyledCopyrigth = styled.span`
	padding: 8px 12px;
	border-radius: 8px;
	background-color: ${(props) => props.theme.COLORS.colorForLightBackground};
`;
